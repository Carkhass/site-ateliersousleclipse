name: Build & Deploy Astro to OVH

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.18.0'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci
        working-directory: ./site-ateliersousleclipse

      - name: Build Astro site
        run: npm run build
        working-directory: ./site-ateliersousleclipse

      - name: Vérifier que dist existe
        run: |
          echo "📂 Contenu du dossier dist :"
          ls -la ./site-ateliersousleclipse/dist || (echo "❌ dist introuvable" && exit 1)

      - name: Deploy via SFTP to OVH
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
          server: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USER }}
          password: ${{ secrets.SFTP_PASSWORD }}
          port: ${{ secrets.SFTP_PORT || 22 }}
          local_path: ./site-ateliersousleclipse/dist
          remote_path: /www
          sftp_only: true
          delete_remote_files: true

      - name: Deployment complete
        run: echo "✅ Déploiement terminé sur https://ateliersousleclipse.fr"